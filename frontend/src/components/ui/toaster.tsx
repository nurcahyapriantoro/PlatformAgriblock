import React from "react";
import { Toast, ToastClose, ToastDescription, ToastTitle } from "./toast";

// A simplified toaster component that doesn't actually display toasts
// This is just a placeholder to satisfy import requirements
export function Toaster() {
  // In a real implementation, this would render active toasts
  return null;
} 