export interface TransactionFilters {
  search?: string;
  startDate?: string;
  endDate?: string;
  status?: string;
  type?: string;
  productId?: string;
  userId?: string;
  publicKey?: string;
} 